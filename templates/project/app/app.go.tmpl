package app

import (
	"github.com/smtdfc/photon/core"
	httpGatway "github.com/smtdfc/photon-http-gateway"
)

func Start() {
	app := core.CreateApp()
	app.SetGateway(
		"http",
		httpGatway.New(httpGatway.Config{}),
	)
	
	InitModules(app)
	app.Start("3000")
}
